use image::{ImageBuffer, Rgb};
use crate::constants::*;
use crate::Result;

pub fn generate_sparkline(data: &[f64], color: (u8, u8, u8)) -> Result<String> {
    // TODO: Implement sparkline generation
    todo!("Implement sparkline generation")
}